// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Task {
  id          Int       @id @default(autoincrement())
  title       String
  description String?
  done        Boolean   @default(false)

  // NEW
  dueDate     DateTime?
  imageUrl    String?

  // Edges in the dependency graph (for Part 3 — ok to leave unused for now)
  dependenciesFrom TaskDependency[] @relation("FromTask")
  dependenciesTo   TaskDependency[] @relation("ToTask")

  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model TaskDependency {
  id     Int  @id @default(autoincrement())
  fromId Int
  toId   Int

  from   Task @relation("FromTask", fields: [fromId], references: [id])
  to     Task @relation("ToTask",   fields: [toId],   references: [id])

  @@unique([fromId, toId])
}

model Todo {
  id        Int      @id @default(autoincrement())
  title     String
  // NEW:
  dueDate   DateTime?
  imageUrl  String?

  createdAt DateTime @default(now())
}
